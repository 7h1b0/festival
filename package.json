{
  "name": "festival",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "format:check": "prettier --list-different \"src/**/*.js\"",
    "prepare": "husky install",
    "pre-commit": "lint-staged",
    "cypress": "cypress"
  },
  "dependencies": {
    "html-minifier-terser": "^7.1.0",
    "vite": "^4.4.11"
  },
  "devDependencies": {
    "@testing-library/cypress": "^9.0.0",
    "@types/testing-library__cypress": "^5.0.9",
    "cypress": "^12.2.0",
    "lint-staged": "^14.0.1",
    "husky": "^8.0.1",
    "prettier": "^3.0.3"
  },
  "lint-staged": {
    "*.{js,svelte,html,css}": "prettier --write"
  }
}
